{{#each challenges as |challenge|}}
  <ChallengeListItem
    @id={{challenge.id}}
    @route={{challenge.route}}
    @isActive={{challenge.isActive}}
    @label={{challenge.menuItemLabel}}
    @handleClick={{action this.navigateToItem}}
  ></ChallengeListItem>
{{/each}}